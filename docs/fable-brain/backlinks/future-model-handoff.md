# Future Model Handoff

Last updated: 2026-07-06 (system creation, Fable 5). **Update this file at the end
of every session that touches backlink work.**

## Where to look first

1. `README.md` (this folder) → 2. `backlink-policy.md` (absolute rules) →
3. `backlink-opportunities.csv` (the working pipeline — sort by Priority, work
`new`/`qualified` rows) → 4. `weekly-backlink-system.md` (what to actually do
today) → 5. this file for current state.

## What was already done (don't redo)

- Full system created 2026-07-06: all 15 files, grounded in verified recon (not
  templates). Trojan identified (book + live funnel at trojanbook.com); Z&E store
  audited via Shopify API + live fetch; SGD competitor/off-site research done
  2026-07-05 with URLs on file.
- 39 prospect rows seeded in the CSV — every one has a real verified URL.
  Competitor patterns mapped in `competitor-backlink-map.md` (confirmed vs assumed
  separated).
- SGD on-site linkable assets inventoried — the PM/commercial guides ALREADY EXIST
  (don't rebuild them; outreach with them).
- SGD site itself is heavily optimized (see `docs/fable-website-brain/` — its own
  audit trail). Z&E education blog exists and is good.

## What still needs doing (the actual work queue)

`implementation-roadmap.md` → "DO THIS FIRST" 10. In one line: owner unblocks →
citations/foundational listings → Z&E review platform → first outreach waves
(SGD property-manager lane, Z&E retailer-review lane) → assets per the build
queue → editorial/PR lanes once proof exists.

## Questions already answered (don't re-ask)

- Trojan = finished influence-psychology book by pen-name "James Lee", funnel at
  trojanbook.com, paid-social primary, Trojan II drafted. NOT a pre-launch project.
- Z&E = real Shopify store (studs $325–$2,495 + engagement rings ~$2–2.8K, all
  IGI), showroom 62 W 47th St, active blog, NO review platform, sells on eBay too.
- SGD commercial competitor set + page-1 recipe: researched with URLs (see
  competitor map). ThomasNet/IDA/BOMA/IREM/Avetta identified as the trade lanes.
- The system's home is this folder in the SGD Queens Site repo (the owner's main
  working repo) even though it covers three businesses.

## OPEN questions — owner must answer (blocks marked rows in CSV)

1. SGD: which manufacturer dealer/authorized relationships actually exist
   (LiftMaster? Clopay? others)? → gates dealer-locator lane.
2. SGD: citation address policy — show address or service-area/hidden? One
   answer, applied everywhere.
3. SGD: remove/merge the duplicate Suffern GBP listing (owner dashboard task).
4. Z&E: founder names/anecdotes for the story page? Product photography for the
   press kit? Is custom jewelry actually offered?
5. Z&E: budget approvals — Jewelers of America membership, The Knot/WeddingWire
   storefront (paid), Trustpilot tier.
6. Trojan: Amazon/KDP listing live? Pen-name interview policy (audio/video vs
   text-only)? Real-name boundary?
7. All: GSC/GA4 API activation (Post Automation app, `docs/gsc-ga4-setup.md`
   there) — measurement depends on it.

## Rules that must never be violated

The forbidden list + approval matrix in `backlink-policy.md`. Cardinal four:
**never invent data** (contacts, DA numbers, backlink profiles, reviews), **never
send outreach autonomously** (owner sends/approves), **never spend or create
accounts without approval**, **never trade domain safety for a link**.

## What NOT to waste time on

Re-auditing the SGD site (done — see website brain); re-identifying Trojan;
re-crawling Z&E's catalog; Tier-3 citation grinding before Tier 1 is done;
DA-chasing blogs; anything `rejected` in the CSV (reasons are in Notes).

## Session-end checklist for every future model

- [ ] CSV statuses current (every touched row)
- [ ] Weekly log line appended to `backlink-kpi-dashboard.md`
- [ ] New owner-questions added above; answered ones moved to "answered"
- [ ] This file's "what was done / what's next" refreshed
- [ ] Any on-site changes followed the site's own brain rules and were verified
      (SGD: tsc → build → prerender)
