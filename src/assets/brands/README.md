Drop manufacturer logo files here named {slug}-logo.svg
(e.g. liftmaster-logo.svg). See src/components/feature/BrandsServed.tsx.
Each one found at build time replaces that brand's text badge with its logo.
